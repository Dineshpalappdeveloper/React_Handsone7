import { createSlice } from "@reduxjs/toolkit";
import Student from "./Data.jsx";

const StudentSlice = createSlice({
    name:"Student",
    initialState:Student,
    reducers:{

    }
})
export default StudentSlice.reducer;